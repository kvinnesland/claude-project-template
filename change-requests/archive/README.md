# change-requests/archive

Completed and rejected CRs moved here at end-of-session.

- Excluded from session startup context load
- Retained for audit trail and reference
- Searchable with `grep -r "CR-NNN" change-requests/archive/`
